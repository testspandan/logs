{
  "name": "your-app-name",
  "version": "1.0.0",
  "description": "Your app description",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "http": "^1.0.0" // Only include other dependencies here, remove "fs"
  }
}
